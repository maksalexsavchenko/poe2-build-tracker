.env
.env.local
.env*.local
node_modules/
# package-lock.json is committed intentionally for Docker builds
.next/
.DS_Store
*.log
prisma/migrations/

# RePoE (npm run vendor:game-data / Docker) — великі JSON, не в git
public/data/repoe-poe2/

# PoB tree (npm run vendor:pob-tree) — tree.json + сотні PNG
public/data/pob-tree/

# Декоративні PNG-кільця орбіт — не рендеримо їх, виглядали як хаотичні «дужки»
public/data/poe2-tree/orbits/

# Локальні artefacts
tsconfig.tsbuildinfo
.claude/launch.json
